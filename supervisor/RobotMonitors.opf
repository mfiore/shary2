;; -*-Lisp-*-
;;author: Michelangelo Fiore
;;this module contains procedures to monitor the robot (a.k.a. stuff
;;that spark misses for now but that I still need ^_^ )
(

 (defop |isFacing| 
   :invocation(!(RobotMonitors.isFacing @support $result))
   :documentation "returns TRUE if the robot is oriented toward the support"
   :body (
	  )
   )
 		
)
